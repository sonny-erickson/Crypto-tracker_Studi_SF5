{% extends 'base.html.twig' %}

{% block title %}{% trans %}Home{% endtrans %}{% endblock %}

{% block body %}
    <div class="bg-black">
    <!-- Navbar start -->
        <nav class=" nav-transaction navbar navbar-expand bg-black border-bot">
            <div class="container-fluid text-light">
                <a class="nav-item" href="{{ path('home') }}" aria-label="Close"><i class="fas fa-times"></i></a>
                <h2 class="flex-fill">Supprimer un montant</h2>
            </div>
        </nav>
  <!-- Nav End -->
         <div class="remove-page">
             <div class="form-container">
             {{ form_start(form) }}
                 <div class="form-group">
                     <div class="input-group">
                         <span class="input-group-text" id="basic-addon1"><img class="arrow" src="{{asset('assets/images/search.png')}}"></span>
                         {{ form_widget(form.transaction, {'attr': { 'class': 'text-light form-control '}}) }}
                     </div>
                 </div>
                 <div class="text-center btn-padding-15">
                    <button type="submit" class="btn-form">VALIDER</button>
                 </div>
             {{ form_end(form) }}
             </div>
        </div>
    </div>
{% endblock %}